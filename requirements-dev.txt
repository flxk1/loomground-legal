# SPDX-License-Identifier: Apache-2.0
# Concrete dev/CI resolution of the abstract ranges in pyproject.toml. Kept out
# of package metadata so the built distribution stays index-clean. Immutable git
# revisions only — never a floating branch — so a build can be reproduced.
#
# Dependency order (see RELEASING.md): legal is downstream of the whole family.
# It can only resolve + go green once its upstreams are published at the tags
# below. For LOCAL development with the sibling checkouts present, conftest.py
# adds their src/ dirs to the path, so `pytest` runs with no pins installed.
#
# Release tags only (loomground-governance-v0.11.1, loomground-deontic-v0.2.1):
# solver 0.6.0 requires loomground-governance >=0.8,<0.12 and loomground-deontic
# >=0.1,<0.3, so these are the newest releases pip accepts next to it.
loomground-governance @ git+https://github.com/flxk1/loomground-governance@270e11d0bbded46c810b2fe56559323d6dcd969a
loomground-deontic @ git+https://github.com/flxk1/loomground-deontic@35f8eabdc533b275a086b20c8d7a161f4acc46a8
# solver-v0.6.0 (the release tag's commit; the earlier pin f8ac006 carried version
# 0.2.1 but was not the solver-v0.2.1 tag commit). This satisfies the >=0.2,<0.7
# range in pyproject.toml; without it `pip install .` looks for loomground-solver
# on the index, finds nothing, and the whole job fails before a single test runs.
# Every symbol this plane consumes (cross_subsumption, proportionality,
# compose_paths, LEX_CONFLICT_PACK, RelationAlgebra, ESCALATE) is present at this
# revision and the suite passes against it.
loomground-solver @ git+https://github.com/flxk1/loomground-solver@5b72f71ebd1f732b4e713b51de15d3ac565faa96
#
# Released (factual-v0.1.0, epistemic-v0.1.0, norm-v0.1.0) but not consumed by
# this plane; pin the tag's commit once a symbol is imported and a range declared:
# loomground-factual @ git+https://github.com/flxk1/loomground-factual@<sha>
# loomground-epistemic @ git+https://github.com/flxk1/loomground-epistemic@<sha>
# loomground-norm @ git+https://github.com/flxk1/loomground-norm@<sha>
#
tomli==2.2.1 ; python_version < "3.11"
pytest>=7.0
